<%= form_with(model: botstrap4hero, local: true) do |form| %>
  <% if botstrap4hero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(botstrap4hero.errors.count, "error") %> prohibited this botstrap4hero from being saved:</h2>

      <ul>
      <% botstrap4hero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :context %>
    <%= form.text_area :context %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
